{
  // Each snippet must have a name and body, but the prefix and description are optional.
  // The prefix is used to trigger the snippet, but when omitted then the name is used.
  // Use placeholders like $1, $2 or ${1:defaultValue} to define tab stops.
  // The $0 determines the final cursor position.
  // Placeholders with the same value are linked.
  "loader": {
    "body": [
      "import { z } from \"zod\";",
      "",
      "const SomeSchema = z.object({",
      " propertyA: z.string().min(1, \"propertyA is missing\"),",
      " proptertyB: z.string().min(1, \"proptertyB is missing\"),",
      "});",
      "",
      "export type SomeType = z.infer<typeof SomeSchema>;",
      "",
      "export async function loader({ request }: Route.LoaderArgs) {",
      "const response = await fetch(`$1`);",
      "const data = await response.json();",
      "",
      "const result = SomeSchema.safeParse(data);",
      "",
      " if (!result.success || !result.data || result.error) {",
      "  throw result.error;",
      " }",
      "",
      " return {};",
      "};",
      ""
    ],
    "description": "RR7 loader Snippet"
  },
  "action": {
    "body": [
      "import { z } from \"zod\";",
      "",
      "const SomeSchema = z.object({",
      " propertyA: z.string().min(1, \"propertyA is missing\"),",
      " proptertyB: z.string().min(1, \"proptertyB is missing\"),",
      "});",
      "",
      "export type SomeType = z.infer<typeof SomeSchema>;",
      "",
      "export async function action({ request }: Route.ActionArgs) {",
      "const response = await fetch(`$1`);",
      "const data = await response.json();",
      "",
      "const result = SomeSchema.safeParse(data);",
      "",
      " if (!result.success || !result.data || result.error) {",
      "  throw result.error;",
      " }",
      "",
      " return result.data;",
      "};",
      ""
    ],
    "description": "RR7 action Snippet"
  },
  "default": {
    "body": [
      "export default function ${1:Unknown}() {",
      " return (",
      "  <div>",
      "    <h1>${1:Unknown} Route</h1>",
      "  </div>",
      " );",
      "}",
      ""
    ],
    "description": "RR7 default Snippet"
  },
  "headers": {
    "body": [
      "export const headers: Route.HeadersFunction = ({",
      "  loaderHeaders,",
      "}: Route.HeadersArgs) => ({",
      "  \"Cache-Control\": loaderHeaders.get(\"Cache-Control\") ?? \"\",",
      "});",
      ""
    ],
    "description": "RR7 headers Snippet"
  },
  "links": {
    "body": [
      "export const links: Route.LinksFunction = () => {",
      " return []",
      "};",
      ""
    ],
    "description": "RR7 links Snippet"
  },
  "meta": {
    "body": [
      "export const meta = ({ data }: Route.MetaArgs) => [",
      "  {",
      "    title: \"Title\",",
      "  },",
      "];",
      ""
    ],
    "description": "RR7 meta Snippet"
  },
  "shouldRevalidate": {
    "body": [
      "import { type ShouldRevalidateFunctionArgs } from \"react-router\";",
      "",
      "export function shouldRevalidate({",
      "  defaultShouldRevalidate,",
      "}: ShouldRevalidateFunctionArgs) {",
      "  return defaultShouldRevalidate;",
      "}",
      ""
    ],
    "description": "RR7 shouldRevalidate Snippet"
  },
  "asdfLog": {
    "body": ["console.log(`$1`);", "console.log($1);"],
    "description": "Console log helper"
  },
  "Functional Component (function)": {
    "prefix": "rff",
    "body": [
      "${4:",
      "type ${1:ComponentName}Props = {",
      "  ${2:propName}: ${3:type};",
      "\\}",
      "}",
      "export default function ${1:ComponentName}(${4:{ ${2:propName} \\}: ${1:ComponentName}Props}) {",
      "  return (",
      "    <div>",
      "      $0",
      "    </div>",
      "  );",
      "}"
    ],
    "description": "TS: React Functional Component"
  },
  "page": {
    "prefix": "page",
    "body": [
      "import type { Route } from \"./+types/${1:route}\";",
      "import { createErrorBoundary } from \"~/components/BaseErrorBoundary\";",
      "import {",
      "  getSessionToken,",
      "  createAction,",
      "  buildPageTitle,",
      "${2:  notFoundResponse,",
      "  noAccessResponse,",
      "  hasChild,",
      "  getSortBy,",
      "  getSortOrder,",
      "  formatRelativeTime,",
      "}",
      "} from \"~/utils\";",
      "import { href${3:, redirect, Link, Outlet, useLocation} } from \"react-router\";",
      "import { getDb${4:, transact} } from \"~/db\";",
      "${5:import z from \"zod\";}",
      "${6:import { jsonArrayFrom } from \"kysely/helpers/postgres\";}",
      "${7:import { sql } from \"kysely\";}",
      "",
      "${8:export function meta() {",
      "  return [{ title: buildPageTitle(\"${9:Page Title}\") }];",
      "}}",
      "",
      "export async function loader({ params, request }: Route.LoaderArgs) {",
      "  const token = getSessionToken(request.headers);",
      "  const db = await getDb();",
      "${10:  const searchParams = new URL(request.url).searchParams;}",
      "",
      "${11:  // Access check + data fetch in single query",
      "  const result = await db",
      "    .selectFrom(\"sessions\")",
      "    .innerJoin(\"workspace_access\", \"workspace_access.user_id\", \"sessions.user_id\")",
      "    .innerJoin(\"workspaces\", \"workspaces.id\", \"workspace_access.workspace_id\")",
      "    .where(\"sessions.token\", \"=\", token)",
      "    .where(\"workspaces.slug\", \"=\", params.slug)",
      "    .where(\"workspace_access.revoked_at\", \"is\", null)",
      "    .innerJoin(\"user_roles\", \"user_roles.id\", \"workspace_access.role_id\")",
      "    .select([",
      "      \"workspaces.id as workspace_id\",",
      "      \"sessions.user_id\",",
      "${12:      // TODO: add selects",
      "    ])",
      "    .executeTakeFirst();",
      "",
      "  if (!result) {",
      "    console.warn(\"Workspace not found or user does not have access\", { slug: params.slug });",
      "    throw notFoundResponse();",
      "  }",
      "",
      "  return {",
      "${13:    // TODO: return loader data",
      "  };",
      "}}",
      "",
      "${14:const actionInfo = createAction(",
      "  ${15:z.object({",
      "    ${16:// TODO: define action schema}",
      "  }),",
      ");",
      "",
      "export async function action({ params, request }: Route.ActionArgs) {",
      "  const token = getSessionToken(request.headers);",
      "  const body = await actionInfo.getBody(request);",
      "  const db = await getDb();",
      "",
      "${17:  // Access check",
      "  const userWorkspace = await db",
      "    .selectFrom(\"sessions\")",
      "    .innerJoin(\"workspace_access\", \"workspace_access.user_id\", \"sessions.user_id\")",
      "    .innerJoin(\"workspaces\", \"workspaces.id\", \"workspace_access.workspace_id\")",
      "    .where(\"sessions.token\", \"=\", token)",
      "    .innerJoin(\"user_roles\", \"user_roles.id\", \"workspace_access.role_id\")",
      "    .where(\"workspaces.slug\", \"=\", params.slug)",
      "${18:    .where(\"user_roles.can_edit_settings\", \"=\", true)}",
      "    .where(\"workspace_access.revoked_at\", \"is\", null)",
      "    .select([\"workspaces.id as workspace_id\", \"sessions.user_id\"])",
      "    .executeTakeFirst();",
      "",
      "  if (!userWorkspace) {",
      "    console.warn(\"User does not have permission for action\", { workspaceSlug: params.slug });",
      "    throw noAccessResponse();",
      "  }",
      "",
      "${19:  // TODO: implement action logic",
      "",
      "  return { success: true };",
      "}}",
      "",
      "export default function ${20:Page}Route({ ${21:loaderData, }params }: Route.ComponentProps) {",
      "${22:  const location = useLocation();}",
      "${23:  const callAction = actionInfo.useAction(href(\"/w/:slug/${24:path}\", { slug: params.slug }));}",
      "",
      "  return (",
      "    <div className=\"@container size-full overflow-auto\">",
      "${25:      <div className=\"flex min-h-full flex-col @sm:justify-center\">",
      "        <div className=\"@sm:px-3\">",
      "          <div className=\"mx-auto flex max-w-2xl flex-col gap-6 p-4 @sm:rounded-xl @sm:border\">",
      "            <h1 className=\"text-xl font-semibold\">${26:Page Title}</h1>",
      "            ${27:// TODO: implement component content}",
      "          </div>",
      "        </div>",
      "      </div>}",
      "    </div>",
      "  );",
      "}",
      "",
      "export const ErrorBoundary = createErrorBoundary(\"${28:page}\");"
    ],
    "description": "React Router page with loader, action, component, meta, and error boundary"
  }
}
